# Decisions — config-data-upgrade

## [2026-03-26] User Decisions (verbatim)

1. `offer_in_phases` → **加到xlsx表里** (Task 1: new column in 道具表.xlsx)
2. 骰子加倍卡 timing → **以xlsx为准 (pre_move)** (Tasks 3-5: engine support needed)
3. 道具文案 → **全部覆盖为xlsx值** (no manual text preservation)
4. vehicles.lua → **扩展导出工具** (Task 6: add vehicles sheet reader)
5. skins.lua → **生成并提交** (Task 10: generate and commit)

## [2026-03-26] Guardrails (Must NOT do)

- 不修改道具效果逻辑 (post_effects.lua)
- 不修改地块、机会卡的游戏规则
- 不删除 constants 中的硬编码默认值
- 不要新增常量到 xlsx (exporter 不支持)
- name_to_key mapping in export tool: only 5 fixed constants, do NOT extend

## [2026-03-26] Architecture Decisions

- `pre_move` phase: sits between roll and move phases in turn flow
- `pre_move` items: use `item_phase_queue`, NOT a new `timing.pre_move_phase` field
- skins.lua: new file, generated by export tool, then committed
