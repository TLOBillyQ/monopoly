# UI：加载屏

对应策划：进入地图/初始化期间展示加载提示。

## Root

- `Canvas_Loading`（`ECanvas`）

## 层级结构（命名 / 类型）

```text
Canvas_Loading (ECanvas)
└─ Img_Loading_Root (EImage)
   ├─ Img_Loading_BG (EImage)
   └─ Lbl_Loading_Tip (ELabel)                    # “加载中…”
```

