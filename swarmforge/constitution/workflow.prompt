# 工作流规则

## 分支和工作树布局

写在 `swarmforge/swarmforge.conf` 里：

| 角色      | 后端    | 工作树                  | 分支                    |
|-----------|---------|-------------------------|-------------------------|
| architect | claude  | 主工作目录              | 当前检出分支（`main`）  |
| coder     | codex   | `.worktrees/coder`      | `swarmforge-coder`      |
| reviewer  | codex   | `.worktrees/reviewer`   | `swarmforge-reviewer`   |
| logger    | none    | 主工作目录              | （只 tail 日志的工具角色） |

启动时每个 agent 要做三件事：

1. 查 `.swarmforge/sessions.tsv` 和 `git rev-parse --abbrev-ref HEAD`，确认自己被分到哪个工作树、哪个分支。
2. 记住这个分配，整个会话不变。
3. 拒绝在别的分支或工作树里干活。布局不对就停下来报告，不准自己悄悄修。

## 交接链

`architect` → `coder` → `reviewer` → （`coder` 跟进 | `architect` 合入 main）。

交接一律用 `swarmtools/notify-agent.sh <role> "<message>"`。每条通知必须带上：

- 分支名和该分支最新的 commit hash。
- 一行变更摘要。
- 跑了哪些门禁、结果如何（比如 `lint: pass; busted behavior: pass; busted guards: pass`）。
- 下一个角色需要拿主意的事。

## 合并方向

Coder 开工前从 `swarmforge-architect`（或 `main`，看架构师提交在哪）合并。Reviewer 审查前从 `swarmforge-coder` 合并。Architect 只有收到 reviewer 通知说门禁全过了，才把变更合入 `main`。

只从产出变更的那个角色的分支合并，别乱拉。

## 纪律

- 每轮 coder 迭代只做一个场景或一个小切片，范围要膨胀就停。
- Reviewer 可以为可测试性重构，但行为不能变。Reviewer 的改动要回给 coder 确认，不准默认接受。
- 无关的本地编辑和生成产物不要提交。
- 没有人类点头，不准跳 hook（`--no-verify`）或签名（`--no-gpg-sign`）。
- 人类用户说了算，可以书面覆盖任何规则。
