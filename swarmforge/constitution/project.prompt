# 项目规则 — 大富翁（蛋仔派对大富翁）

## 身份

Lua 5.5 小游戏，跑在蛋仔派对（Eggy）宿主上。清洁架构，七层加一个横切的 `foundation/`。边界和物理映射见 `docs/architecture/boundaries.md`、`docs/architecture/layer-model.md`，决策 D1–D7 见 `docs/decisions/0001-seven-layer-with-foundation.md`。

默认分支是 `main`，不是 `master`。`swarmforge.conf` 里写 `master` 只是说"架构师在主工作目录里跑，用当前检出的分支"，别把 `main` 改名。

## 硬规则

- 命名：文件和标识符 `snake_case`，类表 `CamelCase`。
- `src/` 里禁止 `tonumber` 和 `type(x) == "number"`，用 `src.foundation.lang.number` 的 `NumberUtils`。见 `.agents/conventions/coding.md`。
- Eggy `Fixed` 参数写浮点，`30.0` 不是 `30`。见 `.agents/conventions/eggy-types.md`。
- `docs/reference/eggy/`（第三方宿主文档）和 `docs/product/design-source/`（xlsx/docx 原件）不要碰。
- `docs/reports/` 是生成的，别手改。超过 30 天的报告只供参考，不算契约。

## 目录结构（别加新的顶层目录）

- `src/` — 生产代码，按七层 + `foundation/` 组织。
- `spec/` — 测试，按 `.busted` 里的 `behavior` / `contract` / `guards` / `tooling` / `regression` profile 分。
- `tools/` — 质量和重构工具。
- `docs/` — architecture / decisions / guides / reports / reference / product，要有 front-matter（`kind` / `status` / `owner` / `last_verified`）。
- `.agents/` — agent 专用的约定和技能。

## 拿不准的时候

去 `.agents/README.md` 找对应的行，照它说的做。别拿这份 prompt 猜代码库长什么样，去读实际文件。
