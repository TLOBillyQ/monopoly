# 工程规则

## 质量门禁

交接前，你的工作树得过这几关：

1. **Lint**，`lua tools/quality/lint.lua`，零警告。
2. **测试**，`busted --run <profile>`，跟变更相关的 profile 都要跑。Profile 定义在 `.busted` 里：
   - `default` — 全量（所有 spec 根目录）。
   - `behavior` — `spec/behavior/`（游戏玩法、领域、运行时、表现层）。
   - `contract` — `spec/contract/`（API 和边界契约）。
   - `guards` — `spec/guards/`（架构 lint、依赖规则）。
   - `tooling` — `spec/tooling/`（script_tools_contract 之类）。
   - `regression` — `spec/regression/`（重放式用例）。
3. **架构扫描**，`src/` 边界动了的话要跑。哪个 profile 管哪类违规，见 `docs/architecture/quality-map.md`。

某个 profile 跟这次变更无关，交接时明确说，别悄悄跳过。

## 测试纪律

TDD 三定律：没有先写一个失败测试来证明必要性的生产变更，不接受。

`spec/behavior/` 是验收层。每个游戏玩法切片至少有一条 behavior spec，描述用户看得见的结果，不是内部调用顺序。

确定性是死规矩：随机性要设种子，`os.time` 只能藏在时钟端口后面，不准断言挂钟副作用。非平凡逻辑同时要有聚焦的单元测试，两层对不上不行。

## 架构纪律

尊重七层加 `foundation/`。新代码放进跟它职责匹配的层。放不进去，停下来问，别硬塞。

`foundation/` 是唯一允许所有层都依赖的层，同层之间不准横向依赖。

`src/` 必须守住 `project.prompt` 里的 number/Fixed 规则。`guards` profile 就是用来验这个的，跑绿了才算数。

## 工具验证

用 `tmux`、`claude`、`codex`、`git`、`busted`、`luacheck`、`lua tools/quality/*` 或任何辅助工具之前，先看本地 `--help` 或 `docs/guides/`、`.agents/skills/` 里的对应文档，确认调用方式。不准自己编参数。

## 生成产物

`docs/reports/*.md`、`luacov.*`、`*.arch_view`、`*.mutate4lua` 都是生成的，别手改。跟任务无关的本地变更和生成产物不要提交，除非任务明确要求并且交接里写了。
