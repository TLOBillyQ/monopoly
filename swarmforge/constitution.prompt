# 大富翁宪法

本文件优先于所有下属文件。
按顺序读完并遵守：

1. `swarmforge/constitution/project.prompt`
2. `swarmforge/constitution/engineering.prompt`
3. `swarmforge/constitution/workflow.prompt`

两份下属文件冲突时，排在前面的赢。

## 消息排队

一次只处理一条消息。

忙的时候收到新消息，追加到你工作树里的 `pending-messages`。手头的事做完，先处理队列里最早那条，处理完删掉。不准静默丢弃。

## 项目指南针

项目的活文档才是真相来源，不是这份 prompt。

- Agent 路由：`.agents/README.md`
- 人类索引：`docs/README.md`
- 日常约定：`.agents/conventions/{coding,reading,planning,eggy-types}.md`
- 质量地图：`docs/architecture/quality-map.md`
- 顶层护栏：`CLAUDE.md`（仓库根目录）

这里的规则和 `docs/architecture/` 或 `docs/decisions/` 下有主人的稳定文档冲突时，听项目文档的。下次交接时说明冲突，好让宪法跟着更新。
