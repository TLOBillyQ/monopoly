# 指导原则

使用直白简练的现代汉语。

## 可执行计划

编写复杂功能或进行重大重构时，按 `.github/PLANS.md` 使用可执行计划。新任务先清空 `PLAN_CURRENT.md` 再写入。

## 编码纪律

代码修改遵守 `.github/CODING.md`。

## 检索纪律

API 文档检索遵守 `.github/READING.md`。

## 项目上下文

运行环境与 API 检索路径见 `.github/THIS.md`。
